{
  "schema": "afi.analysis-plugin.v1",
  "pluginId": "afi-analysis-pattern",
  "pluginVersion": "2.0.0",
  "implementationVersion": "2.0.0",
  "category": "pattern",
  "description": "Official pattern lane plugin manifest (vendor-neutral, provider-instance-backed): the node reads its explicit providerInstanceRef, and the runtime provider layer resolves the instance and invokes the trusted registered pattern adapter over the candles produced by the technical stage (consumed via the 'candles' port). The node contains no kernel or vendor logic; deterministic given the selected instance's inputs.",
  "inputSchemaRef": "afi.enrichment.technical.v1",
  "outputSchemaRef": "afi.enrichment.pattern.v1",
  "deterministic": true,
  "capabilities": ["provider:instance-backed"],
  "defaultTimeoutMs": 10000,
  "permittedFailurePolicies": ["abort", "degrade"],
  "paramsSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "multiInstance": true,
  "mayFeedScorer": true
}
