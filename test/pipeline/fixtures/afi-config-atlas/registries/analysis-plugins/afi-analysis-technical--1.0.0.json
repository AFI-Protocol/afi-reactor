{
  "schema": "afi.analysis-plugin.v1",
  "pluginId": "afi-analysis-technical",
  "pluginVersion": "1.0.0",
  "implementationVersion": "1.0.0",
  "category": "technical",
  "description": "Official technical-analysis plugin manifest: computes indicator features (EMA/RSI/ATR-style) over the market candles fetched for the validated signal. Deterministic given its inputs; requires a price-feed provider in the operator environment.",
  "inputSchemaRef": "afi.usignal.v1.1",
  "outputSchemaRef": "afi.enrichment.technical.v1",
  "deterministic": true,
  "capabilities": ["provider:price-feed"],
  "defaultTimeoutMs": 10000,
  "defaultRetryPolicy": {
    "maxRetries": 1,
    "retryDelayMs": 500,
    "backoff": "fixed"
  },
  "permittedFailurePolicies": ["abort", "degrade"],
  "paramsSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "candleLimit": {
        "type": "integer",
        "minimum": 1,
        "maximum": 1000,
        "description": "Number of candles fetched from the price feed for indicator computation."
      }
    }
  },
  "multiInstance": true,
  "mayFeedScorer": true
}
