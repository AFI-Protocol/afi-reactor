{
  "schema": "afi.analysis-plugin.v1",
  "pluginId": "afi-analysis-pattern",
  "pluginVersion": "1.0.0",
  "implementationVersion": "1.0.0",
  "category": "pattern",
  "description": "Official pattern-recognition plugin manifest: detects chart/candlestick patterns over the candles produced by the technical stage (consumed via the 'candles' port). Deterministic given its inputs; no external providers.",
  "inputSchemaRef": "afi.enrichment.technical.v1",
  "outputSchemaRef": "afi.enrichment.pattern.v1",
  "deterministic": true,
  "defaultTimeoutMs": 5000,
  "permittedFailurePolicies": ["abort", "degrade"],
  "paramsSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "multiInstance": true,
  "mayFeedScorer": true
}
