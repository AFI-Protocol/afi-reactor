{
  "schema": "afi.analysis-plugin.v1",
  "pluginId": "afi-analysis-sentiment",
  "pluginVersion": "2.0.0",
  "implementationVersion": "2.0.0",
  "category": "sentiment",
  "description": "Official sentiment lane plugin manifest (vendor-neutral, provider-instance-backed): the node reads its explicit providerInstanceRef, and the runtime provider layer resolves the instance and invokes the trusted registered sentiment adapter (keyless or header-BYOK per the selected instance's provider). The node contains no vendor logic and no credential access; external provider calls are NOT deterministic.",
  "inputSchemaRef": "afi.usignal.v1.1",
  "outputSchemaRef": "afi.enrichment.sentiment.v1",
  "deterministic": false,
  "capabilities": ["provider:instance-backed"],
  "defaultTimeoutMs": 10000,
  "defaultRetryPolicy": {
    "maxRetries": 2,
    "retryDelayMs": 500,
    "backoff": "fixed"
  },
  "permittedFailurePolicies": ["abort", "degrade"],
  "paramsSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {}
  },
  "multiInstance": true,
  "mayFeedScorer": true
}
