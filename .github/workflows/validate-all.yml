name: AFI Reactor Validation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    name: Validate AFI Reactor
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: 'npm'

      - name: Create required directories
        run: |
          mkdir -p tmp
          mkdir -p codex
          echo "✅ Created tmp/ and codex/ directories"

      - name: Install dependencies
        run: npm ci

      - name: Run tests (build skipped - requires private afi-core dependency)
        id: validation
        continue-on-error: true
        run: |
          echo "🚀 Running AFI Reactor tests..."
          echo "ℹ️  Note: Full validation (build + codex-lint + simulate-signal + mentor-eval) skipped in CI"
          echo "ℹ️  Reason: Requires afi-core (file:../afi-core) which is a private repo"
          echo "ℹ️  Tests run with jest moduleNameMapper to resolve afi-core imports"
          npm test
          echo "✅ Tests completed"

      - name: Ensure all log files exist
        if: always()
        run: |
          echo "📝 Ensuring required logs exist..."

          # Dummy Codex log
          if [ ! -f "codex/codex.replay.log.json" ]; then
            echo '{"status":"incomplete","isDummyLog":true,"message":"Codex replay missing"}' > codex/codex.replay.log.json
          fi

          # Dummy DAG simulation log
          if [ ! -f "tmp/dag-simulation.log.json" ]; then
            echo '{"status":"incomplete","isDummyLog":true,"message":"DAG simulation missing"}' > tmp/dag-simulation.log.json
          fi

          # Dummy Mentor evaluation log
          if [ ! -f "tmp/mentor-evaluation.json" ]; then
            echo '{"status":"incomplete","isDummyLog":true,"message":"Mentor evaluation missing"}' > tmp/mentor-evaluation.json
          fi

      - name: Upload all validation artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: afi-reactor-validation-artifacts
          path: |
            codex/codex.replay.log.json
            tmp/dag-simulation.log.json
            tmp/mentor-evaluation.json
          retention-days: 30

      - name: Final validation status check
        if: always()
        run: |
          if [ "${{ steps.validation.outcome }}" != "success" ]; then
            echo "❌ Validation failed. Check uploaded artifacts."
            exit 1
          else
            echo "✅ Validation passed successfully!"
          fi