name: Secret Scan

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

permissions:
  contents: read

jobs:
  gitleaks:
    name: Scan for Secrets
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history for comprehensive scanning

      - name: Run basic secret scan
        run: |
          echo "🔍 Running basic secret scan..."
          echo "ℹ️  Note: For advanced scanning with Gitleaks, add GITLEAKS_LICENSE secret"
          echo ""

          # Patterns to detect
          FOUND=0

          # MongoDB connection strings with credentials
          if git log --all -p | grep -E "mongodb(\+srv)?://[^:]+:[^@]+@" | grep -v "username:password"; then
            echo "❌ Found MongoDB connection string with credentials"
            FOUND=1
          fi

          # Generic passwords in URLs
          if git log --all -p | grep -E "://[^:]+:[^@]{8,}@" | grep -v "username:password" | grep -v "user:pass"; then
            echo "❌ Found URL with embedded credentials"
            FOUND=1
          fi

          # AWS keys
          if git log --all -p | grep -E "AKIA[0-9A-Z]{16}"; then
            echo "❌ Found potential AWS access key"
            FOUND=1
          fi

          # Private keys
          if git log --all -p | grep -E "-----BEGIN (RSA |EC |OPENSSH )?PRIVATE KEY-----"; then
            echo "❌ Found private key"
            FOUND=1
          fi

          if [ $FOUND -eq 1 ]; then
            echo ""
            echo "⚠️  Potential secrets detected in git history!"
            echo "Please review and remove any hardcoded credentials."
            echo "See SECURITY.md for guidance."
            exit 1
          fi

          echo "✅ No obvious secrets detected"

